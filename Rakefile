require_relative 'config/environment'
require_relative 'bin/run'
require 'sinatra/activerecord/rake'

desc 'starts a console'
task :console do
  Pry.start
end

desc 'Run that rake yo'
task :run do 
  run
end
